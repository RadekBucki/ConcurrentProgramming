<Window
    Height="650"
    Title="MainWindow"
    Width="800"
    mc:Ignorable="d"
    x:Class="Presentation.View.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:Presentation.ViewModel;assembly=ViewModel">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Label
            Content=" "
            FontSize="20"
            HorizontalAlignment="Left"
            Margin="10,167,0,0"
            VerticalAlignment="Top"
            x:Name="HelloWorld" />
        <Button
            Content="Start"
            FontSize="20"
            HorizontalAlignment="Left"
            Margin="30,107,0,0"
            VerticalAlignment="Top" Height="44" Width="108" x:Name="StartButton" Command="{Binding StartCommand}"/>
        <Rectangle HorizontalAlignment="Left" Height="580" Margin="180,0,0,0" Stroke="Black" VerticalAlignment="Center" Width="580" Fill="Black"/>
        <TextBox HorizontalAlignment="Left" Margin="24,61,0,0" TextWrapping="Wrap" Text="{Binding NumOfBalls}" VerticalAlignment="Top" Width="120" FontSize="20" IsEnabled="{Binding ButtonEnabled}"/>
        <Label Content="Liczba kul&#xD;&#xA;" HorizontalAlignment="Left" Margin="35,21,0,0" VerticalAlignment="Top" Width="98" FontSize="20" Height="35"/>
        <Button Content="Stop" HorizontalAlignment="Left" Margin="30,165,0,0" VerticalAlignment="Top" Height="39" Width="108" Background="Red" FontSize="20" Command="{Binding StopCommand}"/>

    </Grid>
</Window>